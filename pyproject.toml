[project]
name = "finance-tracker-skill"
version = "0.1.0"
description = "OpenClaw / Claude Code skill for the local-first finance tracker"
requires-python = ">=3.11"
dependencies = [
  "httpx>=0.27",
  "matplotlib>=3.9",
  "pillow>=10.0",
]

[tool.uv]
package = false
